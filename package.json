{
  "name": "echo-appointments",
  "version": "1.0.0",
  "description": "Echo Appointments — Scheduling & Appointments SaaS Worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "schema": "wrangler d1 execute echo-appointments --remote --file=schema.sql",
    "schema:local": "wrangler d1 execute echo-appointments --local --file=schema.sql",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241224.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.93.0"
  }
}
